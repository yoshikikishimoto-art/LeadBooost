# akanuma-webapp（TalentFlow）— プロジェクト指示

人材紹介(CA/PCA)向けの進捗・売上管理SPA。プロダクト名は **TalentFlow**。

## ドキュメント
- **要件定義書**: [`docs/要件定義.html`](docs/要件定義.html) — 現状(As-Is)版。実装をリバースエンジニアリングしたもの。
- **意思決定・作業ログ**: [`docs/DECISIONS.md`](docs/DECISIONS.md) — 何を・なぜ決めたかの時系列記録。

## 作業ルール（重要）
- **大きめの作業・意思決定をしたら、必ず `docs/DECISIONS.md` の先頭に1エントリ追記してからコミットする。**
  これがこのプロジェクトの「記憶」であり、別PC・共同開発者と共有される唯一の確実な手段（ローカルのメモリ機能は他端末に同期されないため）。
- 作業は `main` から作業ブランチを切り、PR経由でマージする（README参照）。

## 現状の主要な前提（詳細は要件定義書へ）
- データ保存は localStorage のみ（`talentflow.db.v3`）、サーバ/DBなし。
- 認証は「開発モード」で実質なし。ロール(Admin/マネージャー/サブマネージャー/CA)は表示絞り込みのみ。
- 売上の最小単位は候補者ではなく Application(選考企業)。1候補者が複数社に応募しうる。
